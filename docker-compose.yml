version: '3'

services:
    razor-components:
        image: mewgrammer/cloud-in-a-box:latest
        restart: always
        ports:
            - 443:443
            - 80:80
        networks:
            - mainNet
networks:
    mainNet:
        driver: bridge
